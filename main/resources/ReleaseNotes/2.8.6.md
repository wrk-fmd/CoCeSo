# Coordination Center Software - Release Notes

## Version 2.8.6

**Release date: 2025-07-??**

* Switch to Tomcat 10 for Docker build
* Add docker compose YAML file
* Add `autofocus` for lastname field of treatment view
* Fix map URLs in internal map view
* #137 Add second button in registration for batch processing.
* #137 Set autofocus to lastname field instead of External ID in registration
* #137 Add visual highlighting for newly added patients in registration overview. 
       When a patient is saved, the user is redirected to the overview with the new patient highlighted with green background and animation
* #137 Apply patient highlighting also to patients created via takeover functionality
